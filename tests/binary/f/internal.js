
var ASSERT = require("assert");
var BINARY, section;

section = exports['test [[Get]] Number'] = {};

section['test Returns the Number value of a the byte at the given ' +
'offset.'] = function () {
    // TODO
};

section = exports['test [[Put]] Number'] = {};

section['test Sets the byte value at the given offset.'] = function () {
    // TODO
};

section['test Throws a RangeError if the index is beyond the current ' +
'bounds of the buffer.'] = function () {
    // TODO
};

section['test Implicitly coerces the value to a Number and masks it ' +
'with 0xFF.'] = function () {
    // TODO
};

